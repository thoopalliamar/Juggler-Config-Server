
spring:
 data:
  mongodb:
   database: event
   host: localhost
   port: "27017"
   uri: mongodb://10.20.1.216/event
management:
 endpoints:
  web:
   exposure:
    include: "*"
eureka:
 client:
   service-url:
     defaultZone: http://localhost:9090/eureka
   register-with-eureka: "true"
   fetch-registry: "true"

