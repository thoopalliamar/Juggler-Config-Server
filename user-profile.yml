
spring:
  data:
    mongodb:
      database: userdb
      host: localhost
      port: "27017"
      uri: mongodb://localhost/userdb
      username: ""
  application:
    name: UserProfile

eureka:
  client:
    register-with-eureka: "true"
    fetch-registry: "true"
    serviceUrl:
      defaultZone: http://localhost:9083/eureka/
management:
 endpoints:
  web:
   exposure:
    include: "*"
bootstrapid : localhost:9092

